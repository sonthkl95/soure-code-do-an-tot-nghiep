[Unit]
Description=OpenTelemetry Collector
After=network.target

[Service]
Type=simple
ExecStart={{ otelcol_install_dir }}/otelcol-contrib --config={{ otelcol_install_dir }}/config.yml
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
